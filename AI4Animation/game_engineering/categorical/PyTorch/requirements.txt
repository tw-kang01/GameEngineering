# Categorical VQ-VAE + GMD Integration
# pip install -r requirements.txt
#
# PyTorch with CUDA 설치 (먼저 실행):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# PyTorch (CUDA 버전은 위 명령어로 별도 설치)
torch>=2.0.0
torchvision>=0.15.0

# Core dependencies
numpy>=1.24.0
matplotlib>=3.7.0
scikit-learn>=1.3.0
scipy>=1.11.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0.0
pillow>=9.5.0

# ONNX export
onnx>=1.14.0
onnxruntime>=1.15.0
